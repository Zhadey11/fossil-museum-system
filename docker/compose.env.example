# Copia a `compose.env` o dejá que `node docker/prepare.js` lo genere.
# Contraseña del usuario `sa` en el contenedor SQL Server (y la que suelen usar servicios en compose).
# Debe cumplir la política de complejidad de SQL Server.
MSSQL_SA_PASSWORD=FossilMuseum2026!Strong
