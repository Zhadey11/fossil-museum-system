import { ROL } from "./roles";

/** Referencias estables para PanelGuard (evita warnings de hooks). */
export const NEED_ADMIN = [ROL.ADMIN];
export const NEED_INVESTIGADOR = [ROL.INVESTIGADOR];
export const NEED_EXPLORADOR = [ROL.EXPLORADOR];
